---
- name: enable systemd-resolve now
  ansible.builtin.systemd_service:
    name: systemd-resolved
    state: started
    enabled: true
  become: true

- name: configure systemd-resolved mode
  ansible.builtin.include_tasks: systemd-resolved/{{ os_config_systemd_resolved_mode }}.yml
